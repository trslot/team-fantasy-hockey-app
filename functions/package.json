{
  "name": "functions",
  "description": "Firebase Functions for Express server",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-functions": "^4.4.1",
    "axios": "^1.6.0",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}